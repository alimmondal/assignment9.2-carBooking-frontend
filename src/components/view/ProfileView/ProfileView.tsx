"use client";

const ProfileView = () => {
  // const { role } = getUserInfo() as any;
  //   console.log(role);
  return (
    <div>{/* <h1>{role == "admin" ? <p>admin</p> : <p>user</p>}</h1> */}</div>
  );
};

export default ProfileView;
